version: '3.8'

# PostgreSQL is handled by local installation (Homebrew)
# Connection: localhost:5432, database: flowboard_dev

services: {}

# Future services can be added here:
#   redis:
#     image: redis:alpine
#     ports:
#       - "6379:6379"
